# 开发计划20260420

> 计划起始日期：2026-04-20  
> 计划对象：HDY Motion Control Library  
> 计划目标：在不破坏“工艺层 / 运动控制层”边界的前提下，完成基线修复、架构收敛、诊断工业化、嵌入式兼容增强与发布前验证。

---

## 1. 计划原则

本计划按 Sprint 管理，后续所有修改建议遵循以下原则：

1. **不把工艺层逻辑塞回运动控制库。**
2. **先恢复可构建、可测试基线，再做结构优化。**
3. **所有行为变更必须配套自动化测试。**
4. **优先收敛对外契约，再扩展内部实现。**
5. **嵌入式兼容性与可维护性优先于短期堆功能。**

---

## 2. 阶段目标

### 2.1 总体目标

在 6 个 Sprint 内完成以下阶段目标：

- 恢复当前主干构建与测试可用性；
- 收敛 `motion_control` 架构复杂度；
- 建立工业化诊断与保护策略；
- 补齐平台配置与嵌入式适配点；
- 完成测试矩阵增强和文档基线统一；
- 形成可追踪、可回归、可发布的候选基线。

### 2.2 里程碑

| 里程碑 | 目标 | 判定标准 |
| --- | --- | --- |
| M1 | 构建恢复 | `cmake --build --preset unixgcc` 通过 |
| M2 | 回归恢复 | `ctest --test-dir out/build/unixgcc --output-on-failure` 通过 |
| M3 | 架构收敛 | `motion_control` 内部职责清晰，故障上报统一 |
| M4 | 诊断工业化 | 引入滞回、持续时间、抑制与保护策略 |
| M5 | 嵌入式适配增强 | 配置层、精度与时间基具备可裁剪能力 |
| M6 | 发布候选基线 | 文档、测试、构建、发布说明一致 |

### 2.3 截至当前的进度快照（2026-04-20 更新）

- 已完成 P0 基线修复第一阶段：`HDY_ReportFault` 重构遗留已完成收敛，项目已恢复到可构建、可继续回归的基线。
- Direct / Recipe 并存接口已落地：支持 `LoadDirectSegment()` / `ClearDirectSegment()`，启动时按 `USE_RECIPE` 选源，运行时锁存活动段来源，Direct 完成即 `DONE`，Direct 模式下 `NextSegment()` 被显式拒绝并输出诊断。
- 命令语义收敛已取得阶段性进展：`Hold()` / `Resume()` 主路径、非法命令拒绝、诊断确认（Acknowledge）合法性等已有回归测试覆盖。
- `state_reporter` 已补强 live diagnostic 与 `STATE.protectionAction` 的镜像一致性，告警产生时同步保护动作，清除 live diagnostic 时同步清零，避免 HMI / 上位机读到互相矛盾的状态输出。
- 验证状态：历史上下文中已完成全量构建与 `ctest` 回归；本轮增量修改已通过 `test_motion_control` 与 `test_scenario_matrix` 关键回归。
- 当前阶段判断：Sprint 1 已基本收口，Sprint 2 已进入中后段；后续应优先完成“命令/状态/故障契约收敛”，再进入诊断工业化与平台裁剪。

---

## 3. Sprint 划分总览

| Sprint | 时间 | 主题 | 目标 |
| --- | --- | --- | --- |
| Sprint 1 | 2026-04-20 ~ 2026-04-26 | 基线修复与仓库清理 | 恢复构建、恢复测试、消除重构遗留 |
| Sprint 2 | 2026-04-27 ~ 2026-05-03 | 命令/状态/故障路径收敛 | 统一 fault/report 接口，明确 FB 契约 |
| Sprint 3 | 2026-05-04 ~ 2026-05-10 | 诊断与保护工业化 | 建立监视层 + 判据层，减少误报 |
| Sprint 4 | 2026-05-11 ~ 2026-05-17 | 架构轻量化与嵌入式配置 | 收敛 `motion_control` 复杂度，增加平台配置层 |
| Sprint 5 | 2026-05-18 ~ 2026-05-24 | 测试矩阵与场景强化 | 强化故障、边界、噪声、长期回归测试 |
| Sprint 6 | 2026-05-25 ~ 2026-05-31 | 文档基线与发布候选 | 固化文档、验收标准、RC 基线 |

### 3.1 当前建议开发顺序与优先级（最新）

结合当前已完成项与剩余风险，建议后续执行顺序调整为：

1. **优先完成 Sprint 2 收口（当前最高优先级）**  
   聚焦 `T04/T05`：统一故障上报入口、状态落点、命令合法性矩阵、Direct / Recipe / Hold / Resume / Abort / Acknowledge 的契约文档化与测试补齐。
2. **随后进入 Sprint 3 的诊断工业化**  
   在命令和状态语义稳定后，再拆分“监视层 + 判据层”，引入持续时间、滞回、抑制条件与告警/停机分级，避免边开发边返工。
3. **Sprint 4 延后到语义稳定之后再展开**  
   `motion_control` 内部拆分、`hdy_config.h` 配置层、资源裁剪能力都应建立在稳定契约之上；否则容易在接口未定型时做过早抽象。
4. **测试增强工作可前置补充，但正式矩阵强化放在 Sprint 5 收口**  
   当前可以继续按需求补充针对性回归，但大规模噪声、长周期、边界矩阵应在诊断/架构基本稳定后统一展开。
5. **文档与发布候选放在最后统一收敛**  
   避免在核心语义和诊断策略仍变化时频繁重写发布基线文档。

> 最新优先级主线：**先完成命令/状态/故障契约收敛，再做诊断工业化；先固化语义，再做平台抽象；测试与文档全程跟进，但集中收口放在后段。**

---

## 4. Sprint 详细计划

## Sprint 1：基线修复与仓库清理

**时间：2026-04-20 ~ 2026-04-26**  
**目标：恢复可构建、可测试、可继续开发的主干基线。**

### 4.1 主要任务

1. 修复 `motion_control.c` 中所有 `HDY_ReportFault(...)` 遗留调用。
2. 统一故障上报到 `HDY_StateReporter_ReportFault(...)` 或等效内部封装。
3. 重新执行全量构建与全量 CTest，记录失败项与结果。
4. 清理版本管理中的构建产物噪声，收敛 `.gitignore` 与已跟踪输出目录。
5. 检查当前工作树中 `include/pump_converter.h`、`include/state_reporter.h` 等未提交改动是否与主干契约一致。

### 4.2 重点涉及文件

- `src/motion_control.c`
- `src/state_reporter.c`
- `include/state_reporter.h`
- `include/pump_converter.h`
- `.gitignore`
- `CMakeLists.txt`

### 4.3 输出物

- 可通过构建的主干代码；
- 通过的回归测试记录或明确的问题清单；
- 干净的仓库工作区规则；
- Sprint 1 修复说明。

### 4.4 验收标准

- `cmake --build --preset unixgcc` 通过；
- `ctest --test-dir out/build/unixgcc --output-on-failure` 无阻断失败；
- 不再出现 `HDY_ReportFault` 未定义链接错误；
- 构建产物不再污染源码评审范围。

---

## Sprint 2：命令/状态/故障路径收敛

**时间：2026-04-27 ~ 2026-05-03**  
**目标：统一函数块行为语义，降低主控编排逻辑混乱度。**

### 4.5 主要任务

1. 明确并文档化以下接口契约：
   - `LoadRecipe()`
   - `LoadDirectSegment()`
   - `StartSegment()`
   - `NextSegment()`
   - `Hold()` / `Resume()`
   - `Abort()`
   - `Execute()`
2. 将 `motion_control` 中命令入口、状态迁移、运行编排、故障分流逻辑进行内部层次整理。
3. 统一故障后的状态落点、输出安全值、诊断快照与恢复要求。
4. 补充命令非法顺序、空段上下文、时间戳回退、无效反馈等负面测试。

### 4.6 重点涉及文件

- `src/motion_control.c`
- `include/motion_control.h`
- `src/state_reporter.c`
- `src/protection_manager.c`
- `tests/test_motion_control.c`

### 4.7 输出物

- 收敛后的 FB 接口语义说明；
- 更清晰的故障处理路径；
- 新增状态机与错误路径测试；
- Sprint 2 结构整理说明。

### 4.8 验收标准

- 命令触发与周期执行语义一致；
- Hold / Resume / Abort 行为可被测试稳定复现；
- 故障上报入口统一；
- `test_motion_control` 覆盖主要状态转换路径。

---

## Sprint 3：诊断与保护工业化

**时间：2026-05-04 ~ 2026-05-10**  
**目标：把“误差监视”提升为“现场可用的告警/故障判据体系”。**

### 4.9 主要任务

1. 将当前诊断模型拆分为：
   - 监视层（实时误差与状态采样）；
   - 判据层（持续时间、滞回、抑制条件、严重级别、恢复条件）。
2. 按位置、速度、流量、压力分别建立独立阈值策略。
3. 增加启动阶段、段切换阶段、闭环建立阶段的误报抑制逻辑。
4. 明确哪些诊断属于“告警”，哪些属于“停机故障”。
5. 梳理 `HMI诊断对照表.md` 与 `DIAGNOSTIC.code / severity / flags / protectionAction` 的一致性。

### 4.10 重点涉及文件

- `src/diagnostics.c`
- `include/diagnostics.h`
- `src/protection_manager.c`
- `src/state_reporter.c`
- `HMI诊断对照表.md`
- `tests/test_motion_control.c`
- 新增诊断相关测试

### 4.11 输出物

- 工业化诊断判据；
- 告警/故障分级策略；
- HMI 诊断映射更新；
- Sprint 3 诊断策略说明。

### 4.12 验收标准

- 正常瞬态不再频繁触发误报；
- 关键异常能稳定触发同一诊断码；
- `protectionAction` 与故障级别一致；
- HMI 文档与代码输出字段一致。

---

## Sprint 4：架构轻量化与嵌入式配置

**时间：2026-05-11 ~ 2026-05-17**  
**目标：在保持对外接口稳定前提下，提升内部可维护性与平台可移植性。**

### 4.13 主要任务

1. 继续收敛 `motion_control` 内部职责，必要时拆为内部实现单元：
   - command
   - state
   - runtime
   - fault
2. 引入统一平台配置头文件，例如：`hdy_config.h`。
3. 支持以下配置项可裁剪：
   - `HDY_REAL` 精度策略；
   - 时间基类型；
   - 诊断消息字符串开关；
   - 历史深度、最大段数等资源上限。
4. 评估 `pump_converter`、`pressure_controller` 在低资源平台上的精度与性能影响。

### 4.14 重点涉及文件

- `src/motion_control.c` 或其拆分后的内部实现文件
- `include/common_types.h`
- 新增 `include/hdy_config.h`（如采用）
- `src/pump_converter.c`
- `src/pressure_controller.c`
- `CMakeLists.txt`

### 4.15 输出物

- 更清晰的内部架构；
- 平台配置机制；
- 资源可裁剪说明；
- Sprint 4 嵌入式适配说明。

### 4.16 验收标准

- 对外头文件兼容性可控；
- 关键资源上限可集中配置；
- 不影响现有核心测试通过；
- 至少完成一次配置切换验证（如精度或时间基变体）。

---

## Sprint 5：测试矩阵与场景强化

**时间：2026-05-18 ~ 2026-05-24**  
**目标：建立更可信的回归防线，提升复杂工况鲁棒性。**

### 4.17 主要任务

1. 扩展以下测试覆盖：
   - 反馈噪声；
   - 时间戳抖动/回退；
   - 长周期回归；
   - 最大段数边界；
   - 压力步阶切换；
   - Hold/Resume 后继续执行；
   - 故障恢复与 Reset 语义。
2. 强化 `test_scenario_matrix.c` 的典型注塑机动作场景。
3. 为 `pump_converter`、`pressure_controller`、`diagnostics` 增加边界与负面测试。
4. 统计测试覆盖缺口，并形成后续长期补强清单。

### 4.18 重点涉及文件

- `tests/test_motion_control.c`
- `tests/test_pressure_controller.c`
- `tests/test_pump_converter.c`
- `tests/test_scenario_matrix.c`
- `tests/segment_completion_test.c`
- 诊断相关新增测试文件

### 4.19 输出物

- 增强后的测试矩阵；
- 长周期与边界回归记录；
- 关键缺口清单；
- Sprint 5 测试报告。

### 4.20 验收标准

- 核心模块均具备正向 + 负向 + 边界测试；
- 典型注塑机动作链回归通过；
- 长周期运行未出现明显状态漂移或资源异常；
- 测试结果可用于发布前评审。

---

## Sprint 6：文档基线与发布候选

**时间：2026-05-25 ~ 2026-05-31**  
**目标：形成可交付、可追踪、可复盘的 RC 基线。**

### 4.21 主要任务

1. 更新并统一以下文档：
   - `README.md`
   - `项目需求与设计说明书.md`
   - `开发阶段与下一步计划.MD`（重构为历史与当前基线分离）
   - `HMI诊断对照表.md`
   - `正式发布说明模板.md`
2. 明确本版本“纳入发布承诺”与“不纳入范围”的边界。
3. 基于当前结果填写 RC 发布说明初稿。
4. 输出阶段验收清单与回滚建议。

### 4.22 重点涉及文件

- `README.md`
- `项目需求与设计说明书.md`
- `开发阶段与下一步计划.MD`
- `HMI诊断对照表.md`
- `正式发布说明模板.md`
- `项目评审报告.md`
- `开发计划20260420.md`

### 4.23 输出物

- 统一后的项目文档基线；
- RC 发布说明初稿；
- 阶段验收清单；
- 发布候选版本建议。

### 4.24 验收标准

- 文档描述与代码行为一致；
- 测试结果与发布说明一致；
- 上位/HMI/联调方可据此开展对接；
- 具备进入 RC 评审的基础材料。

---

## 5. 任务清单与优先级映射

| 编号 | 任务 | 优先级 | 对应 Sprint | 当前状态 |
| --- | --- | --- | --- | --- |
| T01 | 修复 `HDY_ReportFault` 遗留调用 | P0 | Sprint 1 | 已完成 |
| T02 | 恢复全量构建与 CTest 基线 | P0 | Sprint 1 | 阶段性完成（历史全量回归已恢复；当前增量修改已通过关键回归） |
| T03 | 清理构建产物版本管理问题 | P0 | Sprint 1 | 待复核 |
| T04 | 统一故障上报与状态落点 | P0 | Sprint 2 | 进行中（fault/report 收敛与状态镜像一致性已完成一部分） |
| T05 | 明确 Direct / Recipe / Hold / Resume / Abort 契约 | P0 | Sprint 2 | 进行中（双模式接口与基础命令路径已落地，仍需系统文档化） |
| T06 | 诊断拆分为监视层 + 判据层 | P1 | Sprint 3 | 待开始 |
| T07 | 建立告警/故障分级与保护动作映射 | P1 | Sprint 3 | 待开始 |
| T08 | 收敛 `motion_control` 内部复杂度 | P1 | Sprint 4 | 未开始（依赖 T04/T05/T06 先稳定） |
| T09 | 建立平台配置层与资源裁剪能力 | P2 | Sprint 4 | 未开始（不建议提前抽象） |
| T10 | 增强故障、边界、噪声与长期回归测试 | P1 | Sprint 5 | 未正式开始（已预先补充部分命令/状态回归） |
| T11 | 更新 README / 需求文档 / HMI 文档 | P1 | Sprint 6 | 待开始 |
| T12 | 形成 RC 发布说明与验收清单 | P2 | Sprint 6 | 待开始 |

---

## 6. 每个 Sprint 的 Definition of Done

每个 Sprint 完成时至少满足：

1. 代码已通过本 Sprint 目标相关测试；
2. 新增或修改的行为有对应测试；
3. 文档已同步更新到最小可追踪程度；
4. 不引入新的构建阻断问题；
5. 风险项与未完成项已记录。

---

## 7. 风险与注意事项

### 7.1 技术风险

- 在拆分 `motion_control` 时可能引入行为回归；
- 在工业化诊断时可能导致历史测试预期变化；
- 在引入平台配置层时可能影响头文件兼容性；
- 在清理构建产物时需避免误删真实源码或配置文件。

### 7.2 管控要求

- 每次修改核心状态机前，先补测试；
- 每次修改诊断码或字段前，同步更新 HMI 映射；
- 每次调整对外结构体或枚举时，先评估兼容性影响；
- 不得将工艺层自动切段、阀逻辑、机理联锁重新并入库中。

---

## 8. 建议的周例会检查项

建议每个 Sprint 评审时固定检查以下项目：

- 本周是否仍能全量构建；
- 本周新增测试是否已进入 CTest；
- 是否有新的对外接口变化；
- 是否新增了未在文档说明中的行为变化；
- 是否有现场联调风险未纳入 HMI/保护说明；
- 当前 Sprint 的未完成事项是否明确转移到下个 Sprint。

---

## 9. 跟踪模板

后续可按下表持续更新进度：

| 日期 | Sprint | 任务编号 | 进展说明 | 风险/阻塞 | 下一步 |
| --- | --- | --- | --- | --- | --- |
| 2026-04-20 | Sprint 1 | T01 | 已完成 `HDY_ReportFault` 重构遗留收敛，解除构建阻断 | 需防止后续重构再次引入旧接口调用 | 关闭 Sprint 1 阻断项，保留回归检查 |
| 2026-04-20 | Sprint 1 | T02 | 历史全量构建与 CTest 已恢复；本轮增量修改通过关键回归 | 当前尚未在本轮收尾后再次执行全量验证 | 在进入下一轮结构修改前补做一次全量回归 |
| 2026-04-20 | Sprint 2 | T05 | 已完成 Direct / Recipe 并存接口、运行时来源锁存、Direct 完成即 DONE、Direct 下 NEXT 拒绝 | 接口语义虽已落地，但对外文档与状态矩阵仍需系统整理 | 继续补齐命令契约说明与边界测试 |
| 2026-04-20 | Sprint 2 | T04/T05 | 已补强 Hold / Resume 非法命令路径与 `state_reporter` 的 protectionAction 状态镜像一致性 | 故障/告警最终落点与恢复要求仍需统一定义 | 优先完成故障路径与状态落点收口 |

---

## 10. 计划结论

本计划不以“继续堆功能”为主，而以**恢复基线、收敛架构、提升工业可用性和嵌入式可维护性**为主线。

建议执行顺序当前明确为：

> **先收口 Sprint 2（命令/状态/故障契约），再推进 Sprint 3（诊断工业化）；在语义稳定后再做 Sprint 4 的平台抽象与内部拆分；测试和文档持续跟进，但集中收口放在后段。**

只有在 Sprint 1 ~ Sprint 3 达成后，项目才适合进入更明确的 RC 发布讨论。
