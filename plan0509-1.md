好，基于已读取的全部代码，直接给出完整评审。

---

# `motion_control.c` 与 `motion_interface.c` 设计评审报告

---

## 一、总体评价

| 维度 | motion_control.c | motion_interface.c |
|------|-----------------|-------------------|
| 架构分层 | ✅ 清晰 | ⚠️ 边界渗漏 |
| 状态机设计 | ✅ 严谨 | ⚠️ 状态映射冗余 |
| 封装性 | ✅ 内部私有 | ❌ 大量直访私有字段 |
| 代码复用 | ✅ 良好 | ❌ 严重重复 |
| 注塑场景适配 | ⚠️ 部分缺失 | ⚠️ 硬编码参数 |
| 安全性 | ✅ 防御性好 | ⚠️ 若干隐患 |

**综合评分：motion_control.c 8/10，motion_interface.c 6/10**

---

## 二、`motion_control.c` 详细评审（1725 行）

### 2.1 ✅ 亮点设计

#### 命令合法性矩阵 — 设计精良

```c
// motion_control.c#L56-L84
static const HYD_FbStateMask HYD_COMMAND_ALLOWED_STATE_MASKS[HYD_CMD_ACK + 1U] = {
    [HYD_CMD_START] = HYD_FB_STATE_MASK_BIT(HYD_FB_STATE_IDLE) | ...
    [HYD_CMD_ABORT] = HYD_FB_STATE_MASK_BIT(HYD_FB_STATE_STARTING) | ...
};
```

用 `uint16_t` 位掩码表达状态合法矩阵，O(1) 查找，新增状态/命令只需修改一处，是整个项目里最优雅的设计。

#### 两阶段 Scan 分离 — 符合实时控制范式

`Scan()` = `SampleCommands()` + `Cycle()`，命令采样与执行分离。这保证了：
- 命令在同一周期内不会"自己触发自己"
- 每周期只消费一条待处理命令
- 状态机与 IEC 层的异步时序解耦

#### `HYD_BeginSegment` 的初始化严谨度高

[motion_control.c#L400-L496](src/motion_control.c#L400-L496) 中，段启动时完整执行：
- 重置所有五路诊断判据状态
- 按段容差重新配置判据阈值
- 计算切换抑制窗口
- Bumpless transfer（`PrimeSegmentControllers`）

这是工业级状态机应有的段切换处理。

#### `SoftReset` 的持久化字段保存 — 正确

[motion_control.c#L1462-L1499](src/motion_control.c#L1462-L1499) 中，`SoftReset` 通过栈变量保存配方/增益/判据后全量 `memset`，避免了逐字段清零的遗漏风险。

---

### 2.2 ❌ 问题一：`HYD_CMD_STOP` 和 `HYD_CMD_RESET` 合法状态掩码为零

```c
// motion_control.c#L64, L76
[HYD_CMD_STOP]  = (HYD_FbStateMask)0U,  // 任何状态都拒绝
[HYD_CMD_RESET] = (HYD_FbStateMask)0U,  // 任何状态都拒绝
```

**问题**：STOP 命令在所有状态均不可用，意味着 PLCopen 标准的"减速停止"完全未实现。RESET 靠 `SoftReset()` 直接调用绕开了合法性矩阵，两条命令的存在对状态机而言是死代码。

**影响**：注塑机急停场景只能用 ABORT（立即切零），无减速停止路径，对液压系统产生冲击。

**优化方案**：
```c
// 为 STOP 分配合法状态：STARTING / RUNNING / HOLD
[HYD_CMD_STOP] = HYD_FB_STATE_MASK_BIT(HYD_FB_STATE_STARTING) |
                 HYD_FB_STATE_MASK_BIT(HYD_FB_STATE_RUNNING)   |
                 HYD_FB_STATE_MASK_BIT(HYD_FB_STATE_HOLD),

// RESET 对应单独处理路径，或通过任意状态掩码 0xFFFF 放行
[HYD_CMD_RESET] = (HYD_FbStateMask)0xFFFFU,
```

在 `HYD_MotionControlFB_RunStateMachine` 的 switch 中补充 STOP case，实现按 `segment->maxAcceleration` 减速归零。

---

### 2.3 ❌ 问题二：`RunRunningState` 函数职责过重（约 130 行）

[motion_control.c#L1178-L1307](src/motion_control.c#L1178-L1307) 的 `RunRunningState` 内嵌了：
1. 传感器有效性校验
2. 时间戳回滚检测
3. 执行控制管线（规划器→压力控制器→泵转换器）
4. 五路诊断更新
5. 段完成检测
6. 段完成后的状态转移（自动推进/停止）
7. 仿真反馈写回

**影响**：函数圈复杂度极高，单元测试需要 mock 大量依赖，任何控制路径修改都需在 130 行内定位副作用。

**优化方案**：
```c
// 拆分为职责单一的子步骤
static HYD_BOOL HYD_ValidateFeedbackInputs(HYD_MotionControlFB* fb);
static void     HYD_ExecuteControlPipeline(HYD_MotionControlFB* fb,
                                            const HYD_MotionSegment* seg,
                                            HYD_REAL elapsed);
static void     HYD_EvaluateDiagnostics(HYD_MotionControlFB* fb,
                                         const HYD_MotionSegment* seg,
                                         HYD_REAL elapsed);
static void     HYD_HandleSegmentCompletion(HYD_MotionControlFB* fb,
                                             const HYD_MotionSegment* seg);

static void HYD_MotionControlFB_RunRunningState(HYD_MotionControlFB* fb) {
    if (!HYD_ValidateFeedbackInputs(fb)) return;
    HYD_REAL elapsed = fb->AXIS_REF.timestamp - fb->_segmentStartTime;
    HYD_ExecuteControlPipeline(fb, &fb->_activeSegment, elapsed);
    HYD_EvaluateDiagnostics(fb, &fb->_activeSegment, elapsed);
    HYD_HandleSegmentCompletion(fb, &fb->_activeSegment);
}
```

---

### 2.4 ❌ 问题三：诊断优先级硬编码，遗漏欠压路径

[motion_control.c#L1129-L1152](src/motion_control.c#L1129-L1152)：

```c
if (timeout) {
    priorityCode = HYD_DIAG_CODE_TIMEOUT;
} else if (overPressure) {         // 有 overPressure
    ...
} else if (underPressure) {        // 有 underPressure
    ...
} else if (flowDeviation) { ... }
```

但检查 `underPressure` 的变量`pressureResult` 与 `overPressure` 共用同一个结果体，实际上 `underPressure` 从未被设置为 `true`。搜索全文，`underPressure` 局部变量声明后没有赋值路径，诊断链中欠压检测实质失效。

**影响**：注塑保压阶段液压泄漏导致的欠压故障完全不可检测，这是注塑工艺的高风险缺陷。

**优化方案**：
```c
// 在压力诊断段，区分 over/under 两个方向
if (fb->_errorMonitor.pressureError > pressureTolerance) {
    overPressure = true;
} else if (fb->_errorMonitor.pressureError < -pressureTolerance) {
    underPressure = true;  // 实测低于目标 → 欠压
}
```

---

### 2.5 ⚠️ 问题四：`_switchSuppressEndTime` 仅依赖压力判据参数

```c
// motion_control.c#L481-L482
fb->_switchSuppressEndTime = fb->_pressureCriteria.startupSuppressTime +
                              fb->_pressureCriteria.switchSuppressTime;
```

切换抑制窗口结束时间由**压力判据**的参数决定，但位置/速度模式下压力判据可能未配置（`pressureTolerance=0`），导致 `_switchSuppressEndTime=0`，切换抑制瞬间失效，引起段切换时的虚假诊断告警。

**优化方案**：取各通道最大抑制窗口：
```c
fb->_switchSuppressEndTime = HYD_MaxReal(
    fb->_pressureCriteria.startupSuppressTime + fb->_pressureCriteria.switchSuppressTime,
    HYD_MaxReal(
        fb->_flowCriteria.startupSuppressTime + fb->_flowCriteria.switchSuppressTime,
        fb->_velocityCriteria.startupSuppressTime + fb->_velocityCriteria.switchSuppressTime
    )
);
```

---

### 2.6 ⚠️ 问题五：`HYD_ResumeHeldSegment` 的时间补偿有边界缺陷

```c
// motion_control.c#L584-L589
holdDuration = timestamp - fb->_holdStateTime;
if (holdDuration < 0.0) {
    holdDuration = 0.0;
}
fb->_segmentStartTime += holdDuration;
```

`_segmentStartTime` 不断累加 `holdDuration`，若 HOLD/RESUME 多次执行（注塑机频繁暂停），`_segmentStartTime` 可能大于当前时间戳，导致 `elapsed` 为负，进而让超时判定永远不触发。

**优化方案**：
```c
// 直接重定向段开始时间，而不是累加 hold 时长
// elapsed 基准 = 当前时间 - (原始开始时间 + 累计 hold 时长)
// 更安全的写法：记录累计 holdAccumulated，elapsed 计算时减去
fb->_segmentElapsedAtHold += (timestamp - fb->_holdStateTime);
// 后续 elapsed = (currentTime - fb->_segmentStartTime) - fb->_segmentElapsedAtHold
```

---

## 三、`motion_interface.c` 详细评审（1035 行）

### 3.1 ✅ 亮点设计

#### 两阶段所有权模型 — 解决命令竞争

`_PENDING` → `_EXEC_ID` 两阶段模型优雅地解决了多 FB 实例争用同一轴的仲裁问题：
- EXECUTE 上升沿进入 PENDING：段已提交，等待核心确认
- 核心确认激活后锁定 `_EXEC_ID`：后续周期用 ID 比对判断所有权

这比 PLCopen 标准的单纯 BUSY 检测更可靠，处理了"命令被新命令取代"的竞态。

---

### 3.2 ❌ 问题一：直接访问核心 FB 私有字段，严重破坏封装

`motion_interface.c` 中大量直接访问 `HYD_MotionControlFB` 的私有字段：

```c
// motion_interface.c#L383, L542, L662 等
fb->_executionId              // 私有执行ID
fb->_activeSegmentSource      // 私有段来源
fb->STATE.active              // 内部运行状态
fb->_simFeedback              // 私有仿真反馈
fb->AXIS_REF.timestamp        // 内部时间戳
```

**影响**：`motion_control.c` 的任何内部结构重命名或重构，都必须同步修改 `motion_interface.c`，两个文件形成了隐式紧耦合。这违背了 IEC 适配层仅通过公开 API 与核心引擎通信的设计意图。

**优化方案**：在 [motion_control.h](include/motion_control.h) 补充公开访问器：

```c
// motion_control.h 新增
static inline uint16_t HYD_MotionControlFB_GetExecutionId(
        const HYD_MotionControlFB* fb) {
    return (fb != NULL) ? fb->_executionId : 0U;
}

static inline HYD_SegmentSource HYD_MotionControlFB_GetActiveSegmentSource(
        const HYD_MotionControlFB* fb) {
    return (fb != NULL) ? fb->_activeSegmentSource : HYD_SEGMENT_SOURCE_NONE;
}

static inline HYD_BOOL HYD_MotionControlFB_IsActive(
        const HYD_MotionControlFB* fb) {
    return (fb != NULL) ? fb->STATE.active : false;
}
```

---

### 3.3 ❌ 问题二：三个 Direct 模式 FB 的状态轮询逻辑大量重复

`MoveAbsolute`、`MoveVelocity`、`PressureHandle` 的 PENDING 阶段和 EXEC_ID 阶段的代码结构**完全相同**，约 50 行逻辑在三处复制粘贴：

```c
// 三个函数中完全相同的 PENDING 处理（约 15 行）
if (isPending) {
    if (fb->STATE.active && fb->_activeSegmentSource == HYD_SEGMENT_SOURCE_DIRECT) {
        __SET_VAR(data__->, _EXEC_ID, , (IEC_WORD)fb->_executionId);
        __SET_VAR(data__->, _PENDING, , false);
        ...
    } else if (fb->FB_STATE == HYD_FB_STATE_ABORTED && !fb->STATE.active) {
        __SET_VAR(data__->, COMMANDABORTED, , true);
        ...
    }
}
```

**影响**：三处同步维护，任何仲裁逻辑修改需改三处，遗漏一处即产生行为不一致 Bug。

**优化方案**：提取公共仲裁辅助结构和函数：

```c
// 新增公共仲裁上下文
typedef struct {
    IEC_BOOL*  pBusy;
    IEC_BOOL*  pActive;
    IEC_BOOL*  pCommandAborted;
    IEC_BOOL*  pPending;
    IEC_WORD*  pExecId;
    IEC_BOOL*  pExecute0;
    // 各 FB 特有的 inVelocity / inPressure / done 由回调处理
} HYD_DirectFbOutputs;

// 提取公共 PENDING / EXEC_ID 判断逻辑
static HYD_DirectPhase HYD_Direct_HandleOwnership(
        HYD_MotionControlFB* fb,
        HYD_DirectFbOutputs* out,
        IEC_BOOL isPending,
        IEC_WORD myExecId);
```

---

### 3.4 ❌ 问题三：`INVELOCITY` 和 `INPRESSURE` 的到达判定硬编码

```c
// motion_interface.c#L692-L697 MoveVelocity 的 INVELOCITY 判定
HYD_REAL velError = fb->AXIS_REF.velocity - targetVelocity;
if (velError < 0.0f) velError = -velError;
if (targetVelocity > 0.0f && velError < targetVelocity * 0.05f)  // 硬编码 5%
{
    __SET_VAR(data__->, INVELOCITY, , true);
}

// motion_interface.c#L862-L866 PressureHandle 的 INPRESSURE 判定
HYD_REAL pressError = fb->AXIS_REF.pressure - targetPressure;
if (pressError < 0.0f) pressError = -pressError;
if (targetPressure > 0.0f && pressError < 0.5f)  // 硬编码 0.5MPa
```

**影响**：
- `velError < targetVelocity * 0.05f`：目标速度 100mm/s 时容差 5mm/s，目标速度 5mm/s 时容差 0.25mm/s——比例容差对低速注射（慢速 1-2mm/s）完全不适用
- `pressError < 0.5f`：不同机型的保压精度要求差异巨大（精密件可能要求 ±0.1MPa），硬编码 0.5MPa 无法适应

**优化方案**：容差从段配置读取，而非硬编码：

```c
// MoveVelocity INVELOCITY 判定
HYD_REAL velTol = (fb->_activeSegmentValid && fb->_activeSegment.velocityTolerance > 0.0f)
                   ? fb->_activeSegment.velocityTolerance
                   : targetVelocity * 0.05f;  // 降级为比例容差
if (velError < velTol) {
    __SET_VAR(data__->, INVELOCITY, , true);
}

// PressureHandle INPRESSURE 判定
HYD_REAL pressTol = (fb->_activeSegmentValid && fb->_activeSegment.pressureTolerance > 0.0f)
                     ? fb->_activeSegment.pressureTolerance
                     : 0.5f;
if (pressError < pressTol) {
    __SET_VAR(data__->, INPRESSURE, , true);
}
```

---

### 3.5 ❌ 问题四：Direct 模式 FB 构建段时的硬编码参数不可配置

```c
// motion_interface.c#L86-L92 buildPositionSegment
seg.maxFlow          = (velocity > 0.0f) ? velocity * 0.2f : 50.0f; // 硬编码增益
seg.velocityToFlowGain = 0.2f;     // 固定增益
seg.positionTolerance  = 0.1f;     // 固定 0.1mm
seg.timeoutLimit       = 30.0f;    // 固定 30s

// motion_interface.c#L137-L153 buildPressureSegment
seg.targetFlow       = 5.0f;       // 固定保压流量
seg.maxFlow          = 20.0f;      // 固定最大流量
seg.pressureController = HYD_PRESSURE_CONTROLLER_PI;  // 固定 PI
seg.pressureKp       = 0.5f;
seg.pressureKi       = 0.1f;
```

**影响**：不同注塑机缸径（50~200mm）、不同液压系统排量（20~200cc/rev）、不同工艺精度要求，参数差异可达 10 倍以上。当前实现实际上只能适配一种机型。

**优化方案**：为 Direct 模式 FB 增加可配置参数输入，或通过全局配置接口注入：

```c
// HYD_MOVEABSOLUTE 增加可选配置输入
__DECLARE_VAR(REAL, POSITION_TOLERANCE)   // 0 = 使用默认值
__DECLARE_VAR(REAL, TIMEOUT)              // 0 = 使用默认值
__DECLARE_VAR(REAL, FLOW_GAIN)            // 0 = 使用全局增益

// buildPositionSegment 从 data__ 读取参数，0 时回退默认
seg.positionTolerance = (posTol > 0.0f) ? posTol : 0.1f;
seg.timeoutLimit      = (timeout > 0.0f) ? timeout : 30.0f;
```

---

### 3.6 ⚠️ 问题五：`_PENDING` 状态无超时保护

```c
// motion_interface.c#L540-L555 MoveAbsolute PENDING 阶段
if (isPending) {
    if (fb->STATE.active && fb->_activeSegmentSource == HYD_SEGMENT_SOURCE_DIRECT) {
        // 正常确认路径
    } else if (fb->FB_STATE == HYD_FB_STATE_ABORTED && !fb->STATE.active) {
        // ABORT 路径
    }
    // 否则：永远等待，无超时
    __SET_VAR(data__->, EXECUTE0, , execute);
    return;
}
```

若核心引擎因异常（配置错误、传感器故障导致立即进入 FAULT）未能进入 `STATE.active`，也未触发 `ABORTED`，FB 将在 PENDING 状态无限期挂起，`BUSY=TRUE` 但无任何输出变化，对 PLC 程序而言是死锁。

**优化方案**：
```c
// FB 私有字段增加 PENDING 超时计数
__DECLARE_VAR(UINT, _PENDING_TIMEOUT_CNT)  // 周期计数

if (isPending) {
    IEC_UINT cnt = __GET_VAR(data__->_PENDING_TIMEOUT_CNT);
    cnt++;
    if (cnt > 100U) {  // 100ms（假设 1ms 周期）超时
        __SET_VAR(data__->, COMMANDABORTED, , true);
        __SET_VAR(data__->, BUSY,           , false);
        __SET_VAR(data__->, ACTIVE,         , false);
        __SET_VAR(data__->, _PENDING,       , false);
        __SET_VAR(data__->, _PENDING_TIMEOUT_CNT, , 0U);
        __SET_VAR(data__->, EXECUTE0,       , execute);
        return;
    }
    __SET_VAR(data__->, _PENDING_TIMEOUT_CNT, , cnt);
    ...
}
```

---

### 3.7 ⚠️ 问题六：`__HydMotion_framework_Publish` 中双重调用 `Scan`

```c
// motion_interface.c#L257-L259  Publish 内
for (int i = 0; i < (int)nextAllocatedFB; i++) {
    HYD_MotionControlFB* fb = &HYD_MotionControlFB_inst[i];
    HYD_MotionControlFB_Scan(fb);   // ← 第一次调用
    ...
}

// motion_interface.c#L501-L503  execRising 时 ABORT 后
if (bufferMode == HYD_BUFFER_MODE_ABORT) {
    HYD_MotionControlFB_Abort(fb);
    HYD_MotionControlFB_Scan(fb);   // ← 第二次调用（同周期）
}
```

在 ABORT 路径下，同一个 FB 在同一 PLC 周期内执行了两次 `Scan()`。`Scan()` 内含边沿检测（`_startSegmentSignalPrev`）和命令队列消费，重复执行会使命令被消费两次或边沿信号错乱。

**优化方案**：

```c
// ABORT 后直接调用底层 Cycle()，而不是 Scan()
// Scan = SampleCommands + Cycle，SampleCommands 已在 Publish 中执行过
if (bufferMode == HYD_BUFFER_MODE_ABORT) {
    HYD_MotionControlFB_Abort(fb);
    HYD_MotionControlFB_Cycle(fb);  // 仅执行状态机，不重复采样命令
}
```

---

### 3.8 ⚠️ 问题七：`MoveProfile` 中 Recipe/Direct 模式混用检测缺失

```c
// motion_interface.c#L356-L364
/* Build 1-segment recipe from MOTION if no preloaded recipe */
if (fb->RECIPE_SIZE == 0 && !fb->DIRECT_SEGMENT_VALID) {
    HYD_MotionSegment segment = buildSegmentFromMotion(&motionData);
    if (!HYD_MotionControlFB_LoadRecipe(fb, &segment, 1)) { ... }
}
```

当 `fb->USE_RECIPE = FALSE`（Direct 模式 FB 实例）时，`MoveProfile` 仍会尝试加载 Recipe，逻辑冲突未检测。

**优化方案**：
```c
if (!fb->USE_RECIPE) {
    __SET_VAR(data__->, ERROR,   , true);
    __SET_VAR(data__->, ERRORID, , (IEC_WORD)HYD_DIAG_CODE_COMMAND_NOT_ALLOWED);
    return;
}
```

---

## 四、注塑机应用场景专项问题

### 4.1 V→P 切换（注射转保压）无平滑过渡

注塑机注射段（`MoveVelocity`/`MoveAbsolute`）到保压段（`PressureHandle`）切换时：
- 当前：`MoveAbsolute.DONE=TRUE` → PLC 逻辑 → `PressureHandle.EXECUTE↑`
- 问题：两个 FB 切换至少需要 2 个周期，切换瞬间输出归零，产生压力跌落

**建议**：在 `MoveProfile` 的多段配方中原生支持 MODE 切换段（位置段→压力段），段切换时通过 `PrimeSegmentControllers` 的流量延续实现平滑过渡，而不依赖 PLC 层的双 FB 串联。

### 4.2 注射速度分段（多级注射）依赖 `LOADPROFILE` 空实现

多级注射是注塑工艺的核心需求（典型 3-5 级速度变换），必须通过 Recipe 多段配方实现。当前 `LOADPROFILE` 为空函数体，`MoveProfile` 只能自动构建单段，多级注射完全无法支持。这是当前最高优先级的功能缺口。

---

## 五、优化方案汇总与优先级

| 优先级 | 问题 | 文件 | 估算工作量 |
|--------|------|------|-----------|
| **P0** | 补齐 `LOADPROFILE` 多段配方预加载 | motion_interface.c | 5人天 |
| **P0** | 修复欠压诊断 `underPressure` 未赋值 | motion_control.c | 1人天 |
| **P0** | 修复 `Publish` 中 ABORT 路径双重 `Scan` | motion_interface.c | 0.5人天 |
| **P1** | 实现 `HYD_CMD_STOP` 减速停止 | motion_control.c | 3人天 |
| **P1** | 提取 Direct FB 公共仲裁逻辑，消除重复 | motion_interface.c | 2人天 |
| **P1** | `INVELOCITY`/`INPRESSURE` 改读段容差配置 | motion_interface.c | 1人天 |
| **P1** | Direct 模式段参数可配置化（容差/超时/增益）| motion_interface.c | 3人天 |
| **P1** | 增加核心 FB 公开访问器，消除私有字段直访 | motion_control.h | 1人天 |
| **P2** | `_PENDING` 超时保护 | motion_interface.c | 1人天 |
| **P2** | `_switchSuppressEndTime` 取多通道最大值 | motion_control.c | 0.5人天 |
| **P2** | `ResumeHeldSegment` 时间补偿边界修复 | motion_control.c | 1人天 |
| **P2** | `RunRunningState` 拆分为职责单一子函数 | motion_control.c | 2人天 |
| **P2** | `MoveProfile` 增加 USE_RECIPE 模式预检 | motion_interface.c | 0.5人天 |

---

更多架构背景请参考：

- [指令仲裁与 FB 池](9-command-arbitration-and-fb-pool) — 所有权模型与 PENDING 机制原理
- [配方与直接模式](14-recipe-vs-direct-mode) — 两种运动模式的完整语义
- [执行周期与数据流](22-execution-cycle-and-data-flow) — Scan/Cycle/Publish 调用时序约束
- [诊断监控层](10-diagnostics-monitor-layer) — 五路诊断判据的工作原理