# RBF-PID 理论公式手册

> 本手册系统整理 RBF 径向基神经网络在线整定增量式 PID 控制的全部理论公式，每条公式配「物理含义」与「符号说明」。

---

## 第 0 章 符号约定

| 符号 | 含义 |
|------|------|
| `u(k)` | 控制量绝对值（由 `u(k-1) + Δu(k)` 累加得到） |
| `Δu(k)` | 控制增量 |
| `y(k)` | 被控对象实际输出（压力反馈） |
| `ŷ(k)` | RBF 网络预测输出 |
| `r(k)` | 设定值 |
| `e(k)` | 跟踪误差 = r(k) - y(k) |
| `e_comp(k)` | 补偿误差（Smith 补偿后） |
| `e_rbf(k)` | 辨识误差 = y(k) - ŷ(k) |
| `Kp / Ki / Kd` | PID 三参数 |
| `η_p / η_i / η_d` | PID 参数学习率 |
| `η_w / η_c / η_b` | RBF 权值/中心/宽度学习率 |
| `h_j` | 第 j 个隐含层神经元输出 |
| `c_j` | 第 j 个隐含层神经元中心向量 |
| `b_j`（σ_j） | 第 j 个隐含层神经元宽度 |
| `w_j` | 第 j 个隐含层到输出层权重 |
| `J` | Jacobian 灵敏度；本文统一采用 `∂y/∂Δu` 口径 |
| `m` | 隐含层神经元个数 |
| `a / b` | 离散模型参数 |
| `K / T / T_d` | 对象增益 / 惯性时间 / 纯时滞 |
| `Ts` | 采样周期 |
| `α / β` | 动量系数 |

---

## 第 1 章 被控对象模型

### 1.1 一阶惯性离散模型

```
y(k) = a · y(k-1) + b · u(k)
a = e^(-Ts / T)
b = K · (1 - a)
```

**物理含义**：标准一阶惯性对象的零阶保持离散化形式，用于理论分析与 Jacobian 推导。若以绝对控制量 `u(k)` 为输入，则该模型下 `∂y/∂u = b` 为常数；若工程实现采用增量通道建模，则关注的是相邻采样点上 `Δu` 对输出变化的局部灵敏度。

**符号说明**：`a` 离散衰减系数；`b` 离散增益；`T` 惯性时间常数；`K` 对象静态增益；`Ts` 采样周期。

### 1.2 液压系统连续传递函数（含时滞）

```
P(s) = [ K / (T·s + 1) ] · e^(-T_d · s) · Q(s)
```

**物理含义**：实际液压伺服压力对象的连续模型。一阶惯性 `K/(Ts+1)` 描述管路容腔充放压动态，`e^(-T_d·s)` 描述阀响应与管路传播的纯时滞，`Q(s)` 描述阀非线性、死区、滞环等未建模动态。

**符号说明**：`T_d` 纯时滞时间；`Q(s)` 非线性算子。

---

## 第 2 章 增量式 PID 控制律

```
e(k)  = r(k) - y(k)
Δu(k) = Kp·[e(k) - e(k-1)] + Ki·e(k) + Kd·[e(k) - 2·e(k-1) + e(k-2)]
u(k)  = u(k-1) + Δu(k)
```

**物理含义**：增量式 PID 输出控制量增量 Δu(k)，累加得到绝对控制量 u(k)。天然抗积分饱和（无积分累积项），且切换自动/手动时无冲击。

**符号说明**：`e(k-1)`、`e(k-2)` 为历史误差；`Kp·[e(k)-e(k-1)]` 比例项（实质是误差的差分）；`Ki·e(k)` 积分项；`Kd·[e(k)-2e(k-1)+e(k-2)]` 微分项（二阶差分）。

---

## 第 3 章 性能指标

```
J(k) = ½ · e(k)² = ½ · [r(k) - y(k)]²
```

**物理含义**：瞬时误差二次型性能指标。PID 参数自适应的目标是通过梯度下降使 J(k) 最小化，从而最小化跟踪误差。

**符号说明**：`J` 标量性能指标；`e(k)` 当前跟踪误差。

---

## 第 4 章 RBF 神经网络辨识模型

### 4.1 NARX 辨识结构

```
ŷ(k) = Σ_{j=1}^{m}  w_j · h_j(k)

h_j(k) = exp( -‖x(k) - c_j‖² / (2 · b_j²) )

‖x(k) - c_j‖² = Σ_{i=0}^{n-1} [x(i) - c_{j,i}]²
```

**物理含义**：RBF 网络作为被控对象的在线辨识器，采用 NARX（非线性自回归外生输入）结构，用高斯核函数逼近 y(k) = f(x(k)) 的非线性映射。网络输出 ŷ(k) 逼近真实输出 y(k)。

**符号说明**：`m` 隐含层神经元数；`n` 输入维度；`h_j` 第 j 个高斯核函数输出；`c_j` 第 j 个神经元中心向量；`b_j` 第 j 个宽度参数；`w_j` 第 j 个输出权重；`x(k)` 输入向量。

### 4.2 辨识误差

```
e_rbf(k) = y(k) - ŷ(k)
```

**物理含义**：RBF 网络的预测残差，作为网络在线学习（权值/中心/宽度更新）的训练信号。e_rbf → 0 表示网络已逼近对象动态。

**符号说明**：`y(k)` 实际输出；`ŷ(k)` 网络预测输出。

---

## 第 5 章 Jacobian 矩阵（被控对象灵敏度）

```
∂ŷ/∂x[i] = Σ_{j=1}^{m}  w_j · h_j · (c_{j,i} - x[i]) / b_j²

特别地，当 x[0] = Δu(k) 时：
J = ∂y/∂Δu ≈ ∂ŷ/∂x[0] = Σ_j  w_j · h_j · (c_{j,0} - x[0]) / b_j²
```

**物理含义**：RBF 网络输出对输入第 i 维的偏导数。本文统一采用当前工程实现的 `Δu`-型建模口径：当 `x[0] = Δu(k)` 时，`∂ŷ/∂x[0]` 解释为对象对控制增量的局部灵敏度 `∂y/∂Δu`。这不是经典教材里最常见的 `∂y/∂u` 写法，但对增量式 PID 在线整定是自洽的，因为控制器每一拍真正直接施加到对象上的“新信息”正是本拍控制增量。

**符号说明**：`c_{j,i}` 第 j 个神经元中心向量的第 i 维；`b_j²` 宽度平方。

---

## 第 6 章 RBF 输入向量设计原则

### 6.1 推荐 4 维输入（当前实现口径）

```
x[0] = Δu(k)        // 控制量增量（绝对值，用于求 ∂y/∂Δu）
x[1] = y(k-1)      // 上一时刻实际输出
x[2] = y(k-2)      // 上两时刻实际输出
x[3] = e(k)        // 当前误差（可选，增强设定值跟踪辨识）
网络输出：ŷ(k) ≈ y(k)
```

**物理含义**：本文按当前工程实现统一采用 `Δu`-型输入设计。这样定义后，RBF 网络估计的是“本拍控制增量变化”对对象输出的局部影响，而不是绝对控制量的静态灵敏度。三个核心原则：
1. **x[0] 允许并推荐使用 `Δu(k)`**。原因是增量式 PID 的直接决策量本来就是 `Δu(k)`；将其作为 RBF 第一输入，可让 Jacobian 直接刻画“本次修正量是否有效”；
2. **输入绝不能包含 y(k) 本身**——否则造成目标泄漏（target leakage），网络退化为恒等映射 ŷ(k)=y(k)，Jacobian 失真；
3. **误差 e(k) 可选**——作为辅助输入增强对设定值跟踪特性的辨识。

**为什么允许 `Δu` 作为第一输入：**

`Δu`-型文献路线的核心假设是：在离散控制器中，`u(k-1)` 已是上一拍固定背景量，本拍真正“可学习、可调整”的输入是 `Δu(k)`。因此把 `Δu(k)` 放在 `x[0]`，就能把 Jacobian 定义为 `∂y/∂Δu`，再直接送入增量式 PID 的参数更新律。这条路径与当前实现完全同口径，也更贴合“在线调的是本拍增量，而不是重建整条绝对控制轨迹”的工程目标。

**符号说明**：`Δu(k)` 控制量增量；`y(k-1)`、`y(k-2)` 历史输出；`e(k)` 当前误差。

### 6.2 目标泄漏的数学后果

若 x[1] = y(k)，网络可令 w_1 → 1、其余 w_j → 0，使 ŷ(k) = y(k)（恒等映射）：

```
此时 ∂ŷ/∂x[0] → 0   （Jacobian 失真）
e_rbf = y - ŷ → 0    （看似辨识完美，实则无意义）
```

**物理含义**：目标泄漏使辨识误差虚假归零，但 Jacobian 被噪声主导，PID 自适应失去正确的梯度方向。即便本文采用 `Δu`-型 Jacobian，这一点仍然成立。

### 6.3 `Δu`-型输入与当前实现映射

当前工程实现采用如下近似映射：

```
x[0] = Δu(k-1)
x[1] = y(k)
x[2] = y(k-1)
x[3] = e(k)
```

其中：

- `x[0]` 使用上一拍控制增量，是因为在线更新时可直接复用已落地执行的增量历史；
- `x[1]`、`x[2]` 同时保留当前/上一拍反馈，有利于在强噪声液压系统里快速形成局部动态估计；
- `x[3]` 保留当前误差，用于增强设定变化和扰动工况下的辨识能力。

这套实现比理想的纯历史 NARX 结构更工程化，也引入了额外的泄漏与噪声放大风险，因此它应被视为“与当前代码一致的实现口径”，而不是最严格的辨识理论模板。

---

## 第 7 章 PID 参数自适应调整律

### 7.1 链式法则推导

以 Kp 为例，由梯度下降 ΔKp = -η·∂J/∂Kp，对 J = ½e(k)² 应用链式法则：

```
∂J/∂Kp = ∂J/∂e · ∂e/∂y · ∂y/∂Δu · ∂Δu/∂Kp
       = [e(k)] · [-1] · [∂y/∂Δu] · [∂Δu/∂Kp]
```

对增量式 PID，`u(k) = u(k-1) + Δu(k)`。在 `Δu`-型理论口径下，自适应关注的是“参数变化如何改变本拍增量”，因此直接取 `Δu(k)` 对参数的偏导：

```
∂Δu(k)/∂Kp = e(k) - e(k-1)
```

故：

```
∂J/∂Kp = -e(k) · (∂y/∂Δu) · [e(k) - e(k-1)]
```

**物理含义**：揭示 PID 参数更新所需的两个关键量——被控对象对控制增量的局部灵敏度 `∂y/∂Δu`（由 RBF Jacobian 提供）与增量式 PID 输出对参数的偏导 `∂Δu/∂Kp`。这与经典 `∂y/∂u` 路线相比，更强调“本拍增量是否在改善输出”。

**符号说明**：`∂J/∂e = e(k)`；`∂e/∂y = -1`（因 `e = r - y`）；`∂y/∂Δu` 即本文 Jacobian `J`。

### 7.2 参数更新公式

```
ΔKp = η_p · e(k) · J · [e(k) - e(k-1)]
ΔKi = η_i · e(k) · J · e(k)
ΔKd = η_d · e(k) · J · [e(k) - 2·e(k-1) + e(k-2)]

Kp ← Kp + ΔKp
Ki ← Ki + ΔKi
Kd ← Kd + ΔKd
```

其中 `J = ∂y/∂Δu`（由第 5 章 RBF 网络提供）。

**物理含义**：PID 三参数的在线梯度下降更新律。误差 `e(k)` 决定更新方向与幅度；Jacobian `J = ∂y/∂Δu` 决定梯度对对象增量灵敏度的修正；后缀项（差分/误差/二阶差分）分别对应 P/I/D 三个通道的激励。

**符号说明**：`η_p / η_i / η_d` 三参数学习率；`J` Jacobian；`e(k)-e(k-1)` 误差一阶差分；`e(k)-2e(k-1)+e(k-2)` 误差二阶差分。

---

## 第 8 章 RBF 网络在线学习算法

RBF 网络的权值、中心、宽度均由辨识误差 e_rbf 驱动的梯度下降在线更新。

### 8.1 权值更新

```
Δw_j = η_w · e_rbf · h_j
w_j ← w_j + Δw_j
```

**物理含义**：输出层权值的梯度下降。e_rbf 为预测残差，h_j 为隐含层激活值，二者乘积即梯度方向。

**符号说明**：`η_w` 权值学习率。

### 8.2 中心更新

```
Δc_{j,i} = η_c · e_rbf · w_j · h_j · (x[i] - c_{j,i}) / b_j²
c_{j,i} ← c_{j,i} + Δc_{j,i}
```

**物理含义**：隐含层中心向量的梯度下降。中心向输入数据密集区域移动，使基函数覆盖实际工作点。

**符号说明**：`η_c` 中心学习率；`x[i] - c_{j,i}` 输入与中心的偏差。

### 8.3 宽度更新

```
Δb_j = η_b · e_rbf · w_j · h_j · ‖x - c_j‖² / b_j³
b_j ← b_j + Δb_j
```

**物理含义**：宽度参数的梯度下降。宽度决定基函数的感受野大小，自适应调整使网络既能覆盖工作区间又保持分辨率。

**符号说明**：`η_b` 宽度学习率；`‖x - c_j‖²` 输入到中心的欧氏距离平方；`b_j³` 宽度立方。

### 8.4 动量项（加速收敛）

```
// 标准动量（Heavy Ball）
Δw_j ← Δw_j + α · (w_j - w_{j,prev})

// 二次动量（Nesterov 变体）
Δw_j ← Δw_j + α · Δw_{j,k-1} + β · Δw_{j,k-2}
```

**物理含义**：动量项累积历史更新方向，抑制梯度震荡、加速收敛。标准动量只用一阶历史；二次动量额外引入二阶历史，类似 Nesterov 加速梯度。

**符号说明**：`α` 一阶动量系数；`β` 二阶动量系数；`w_{j,prev}` 上一周期权值；`Δw_{j,k-1}`、`Δw_{j,k-2}` 历史更新量。

---

## 第 9 章 Smith 预估器（时滞补偿）

### 9.1 模型输出预测

```
model_out(k) = a · model_out(k-1) + b · u(k - delay_steps)

其中：
delay_steps = ROUND(T_d / Ts)
a = e^(-Ts / T),  b = K · (1 - a)
```

**物理含义**：Smith 预估器用对象模型预测"无时滞"时的输出。`model_out(k)` 是含时滞的模型输出（与实际对象同步），`model_out_without_delay(k)` 是无时滞的模型输出（超前 delay_steps 步）。

**符号说明**：`delay_steps` 时滞步数；`a / b` 离散模型参数。

### 9.2 补偿误差

```
e_comp(k) = r(k) - [ y(k) + model_out_without_delay(k) - model_out(k) ]
```

**物理含义**：用模型预测消除纯时滞对反馈的影响。`model_out_without_delay - model_out` 是时滞段内的模型响应增量，加到实际反馈上等效于"去掉时滞"的反馈。

**符号说明**：`y(k)` 实际测量输出；`model_out_without_delay(k)` 无时滞模型输出；`model_out(k)` 含时滞模型输出。

---

## 第 10 章 在线增益辨识

```
K_est = (P_actual - P_prev) / (flow - flow_prev)        // 稳态时计算
K ← α · K_est + (1 - α) · K                             // 指数移动平均平滑
```

**物理含义**：在稳态工况下，用压力变化量与流量变化量之比在线估计对象静态增益 K。指数移动平均（EMA）平滑抑制噪声，避免增益估计跳变。

**符号说明**：`P_actual / P_prev` 相邻周期压力；`flow / flow_prev` 相邻周期流量（控制量）；`α` 平滑系数（0 < α < 1）。

---

## 第 11 章 公式间逻辑关系

```
被控对象模型 (§1)
    │  实际输出 y(k)
    ▼
增量式 PID 控制律 (§2)  ──▶  误差 e(k)
                              │
                              ▼
                        性能指标 (§3) J = ½e²
                              │  梯度下降 ∂J/∂Kp
                              ▼
                  ┌──────────────────────────────┐
                  │   PID 参数自适应调整律 (§7)    │
                  │   ΔKp = η·e·J·[e-e_prev]     │
                  │   （J = ∂y/∂Δu）              │
                  └──────────────┬───────────────┘
                                 │
              ┌──────────────────▼──────────────────┐
              │   RBF 神经网络辨识 (§4)              │
              │   ŷ = Σ w_j · h_j                   │
              │   辨识误差 e_rbf = y - ŷ             │
              └──────────────┬──────────────────────┘
                             │ e_rbf
                             ▼
              ┌─────────────────────────────────────┐
              │   RBF 在线学习 (§8)                  │
              │   Δw_j = η_w·e_rbf·h_j              │
              │   Δc = η_c·e_rbf·w_j·h_j·(x-c)/b² │
              │   Δb = η_b·e_rbf·w_j·h_j·‖x-c‖²/b³│
              └─────────────────────────────────────┘
                             ▲
                             │ 输入向量设计 (§6)
                             │ x[0]=Δu, x[1]=y(k-1)
                             │ x[2]=y(k-2), x[3]=e
                             ▼
                          Jacobian (§5)
                       J = ∂ŷ/∂x[0] ≈ ∂y/∂Δu
                      （服务于 §7 的参数调整）

补偿支路：
   Smith 预估器 (§9) ──▶ 补偿误差 e_comp
   在线增益辨识 (§10) ──▶ 跟踪对象增益 K 漂移
```

**公式链条要点**：
1. **主回路**：对象 → PID → 误差 → 性能指标 → 参数调整 → 新 PID → 新 u → 新 y（闭环）
2. **辨识回路**：RBF 输入 → 隐含层 → 预测 ŷ → 辨识误差 e_rbf → 更新 w/c/b → 更好的 ŷ → 更准的 J
3. **关键耦合点**：Jacobian `J` 同时服务于"参数调整律"（作为 `∂y/∂Δu`）和"在线学习"（作为网络梯度），是整个算法的核心枢纽
4. **时滞补偿支路**：Smith 预估器与增益辨识作为前馈/补偿环节，改善相位滞后与参数漂移

---

*文档完。*
