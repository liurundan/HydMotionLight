# 项目评审报告

> 项目：HDY Motion Control Library  
> 评审日期：2026-04-20  
> 评审对象：当前工作区源码、测试、设计文档与开发文档  
> 评审结论：**架构方向正确，核心能力已基本成形，但当前基线仍处于“发布前收敛与工业化加固阶段”，尚不宜直接作为稳定发布版本。**

---

## 1. 评审目的

本报告用于对当前液压运动控制库进行一次面向注塑机场景的阶段性审查，重点回答以下问题：

1. 项目需求与当前代码实现是否一致。
2. 当前软件架构是否合理，模块划分是否清晰完整。
3. 代码质量、测试覆盖与嵌入式适配性是否达到可持续演进要求。
4. 下一阶段应优先修复和优化哪些问题。

---

## 2. 评审依据

本次评审主要基于以下材料与现状：

- 《项目需求与设计说明书.md》
- 《开发阶段与下一步计划.MD》
- `README.md`
- `HMI诊断对照表.md`
- `include/` 与 `src/` 下当前头文件与源文件
- `tests/` 下自动化测试代码
- 当前工作区构建与测试结果
- 当前工作区未提交修改状态（包括 `include/pump_converter.h`、`include/state_reporter.h` 及部分构建产物变更）

---

## 3. 总体结论

### 3.1 总体评价

当前项目已经具备较完整的轻量级液压运动控制库骨架，核心优点明确：

- **工艺层 / 运动控制层边界总体正确**，没有把阀逻辑、机构联锁、自动切段决策硬塞进库内部。
- **PLCopen 风格函数块接口已基本成形**，具备 `Init / LoadRecipe / LoadDirectSegment / StartSegment / NextSegment / Abort / Execute` 等核心入口。
- **多段 Recipe、Direct/Recipe 双来源、段完成判定、诊断与状态输出、压力控制、泵速换算**等关键能力已具备。
- **静态内存、固定上限、纯 C99、无动态分配**，符合嵌入式平台的基本约束。

### 3.2 当前阶段判断

建议将当前阶段定义为：

> **核心能力已成形，进入发布前收敛、架构细化、诊断工业化和嵌入式兼容强化阶段。**

### 3.3 阻断性结论

当前工作区存在一个明确的 **P0 阻断问题**：

- 全量构建过程中，`motion_control.c` 中多处仍调用未定义符号 `HDY_ReportFault`；
- 该问题会直接导致链接失败，阻断 `test_motion_control` 等目标构建；
- 说明当前代码存在一次未完全收敛的重构遗留。

在此问题未修复前，**当前工作区不能视为稳定可交付基线**。

---

## 4. 需求符合度分析

## 4.1 与需求一致的部分

### 4.1.1 分层边界基本符合需求

需求文档要求：

- 工艺层负责动作编排、阀逻辑、段切换决策；
- 运动控制层负责数学规划、压力/流量/泵速输出。

当前实现总体保持了这条边界，这是本项目最值得保留的设计原则。

### 4.1.2 多段配方与动作建模能力符合预期

当前数据模型能够表达：

- 多段运动配方；
- 位置、时间、压力、流量等目标；
- 段启动、段切换、段完成与结束状态；
- 合模、开模、射胶、保压、顶针、抽芯等注塑机典型动作链条。

### 4.1.3 函数块式调用习惯已具备

当前接口已经形成较清晰的 FB 调用语义，能够支持：

- 初始化；
- 装载配方或直接段；
- 显式启动段；
- 周期调用执行；
- 读取状态、诊断与执行输出。

这与 IEC61131-3 / PLCopen 风格是一致的方向。

### 4.1.4 嵌入式友好特征较好

当前工程具备以下嵌入式友好特征：

- 无动态内存；
- 固定数组和固定历史深度；
- 纯 C99；
- 模块职责大体可识别；
- 没有明显的操作系统依赖。

## 4.2 与需求仍存在偏差的部分

### 4.2.1 当前工作区构建不可通过

这属于最直接的不符合项。无论架构设计多完整，只要当前基线不能稳定构建，就不能作为工程发布基础。

### 4.2.2 `motion_control.c` 仍承担过多职责

当前 `motion_control.c` 同时承担：

- 命令处理；
- 状态推进；
- 段上下文维护；
- 诊断分流；
- 运行时调度；
- 故障与保护分支处理；
- 多种输入来源编排。

这会造成：

- 单文件过重；
- 状态机难以单测；
- 后续功能扩展时容易继续膨胀；
- 代码理解与维护成本偏高。

### 4.2.3 诊断语义还偏“误差监视”，不够“工业告警”

当前诊断模型已经有良好基础，但离现场可用的工业语义仍有差距，主要体现在：

- 缺少持续时间判据；
- 缺少进入 / 退出阈值滞回；
- 缺少建立阶段屏蔽；
- 缺少“告警”和“停机故障”的清晰策略分层；
- 容易把正常瞬态误差直接反映为告警事件。

这在注塑机液压控制现场容易产生误报和 HMI 噪声。

### 4.2.4 文档基线与代码基线不一致

当前存在多份文档对项目状态描述不一致的问题，例如：

- 架构已拆分的模块，在旧文档中仍以“待独立”状态描述；
- 当前工作区构建失败，但部分历史文档中仍带有“通过测试”的叙述；
- 某些模块状态与接口语义未在 README 和设计说明中统一。

这会降低后续联调、交接和问题追踪效率。

---

## 5. 软件架构评审

## 5.1 架构优点

### 5.1.1 顶层边界合理

当前最关键的优点是：**工艺层逻辑没有侵入运动控制库**。这意味着该库可以作为“控制数学内核”被不同上层流程复用。

### 5.1.2 模块集合已经较完整

从代码结构看，项目已经具备以下主要模块：

- `motion_control`
- `motion_planner`
- `pressure_controller`
- `pump_converter`
- `segment_completion`
- `diagnostics`
- `state_reporter`
- `protection_manager`
- `recipe_validator`
- `ramp_controller`
- `rbf_pid`

对一个面向嵌入式的轻量级控制库来说，这一模块集合已经比较完整。

### 5.1.3 控制链路基本清晰

当前主链路已大致形成：

- `motion_control`：对外函数块入口与运行编排；
- `motion_planner`：位置 / 速度目标规划；
- `pressure_controller`：压力控制策略执行；
- `pump_converter`：流量到泵速转换；
- `segment_completion`：段完成判定；
- `state_reporter`：状态与诊断输出规整；
- `diagnostics` / `protection_manager`：监测与保护支撑；
- `recipe_validator`：配方合法性验证。

这一架构比早期“所有逻辑集中在控制主文件”的状态明显成熟。

### 5.1.4 适配嵌入式平台的总体方向正确

当前设计避免了复杂运行时依赖，结构体承载状态，较适合：

- PLC/软PLC 周期调用；
- 单板控制器嵌入式部署；
- 多实例复用；
- 静态链接与长期维护。

## 5.2 架构不足与优化方向

### 5.2.1 `motion_control` 需要继续内聚化与拆分

建议保持对外仍是统一的 `motion_control.h`，但在内部进一步拆分为：

- `motion_control_command.*`：命令入口、触发与参数校验；
- `motion_control_state.*`：状态转换与状态机规则；
- `motion_control_runtime.*`：周期运行时编排、planner / controller / converter 调用；
- `motion_control_fault.*`：故障分流与安全停机。

这样可以显著改善可读性、可测试性与扩展性。

### 5.2.2 诊断层应拆分为“监视层”和“判据层”

建议形成两层语义：

1. **监视层（monitor）**：只计算当前误差、偏差、越限、状态快照；
2. **判据层（policy）**：根据持续时间、滞回、抑制条件、严重级别决定是否上报为告警或故障。

这是从“实验室可观察”走向“现场可用”的关键一步。

### 5.2.3 平台配置层需要补齐

建议增加统一的平台配置头文件，例如 `hdy_config.h`，集中定义：

- 实数类型选择（`float` / `double`）；
- 时间基类型与换算策略；
- 是否启用详细 message 字符串；
- 诊断历史深度；
- 最大段数等静态资源上限。

这样更有利于不同 MCU / IPC / PLC 目标平台适配。

### 5.2.4 控制策略与接口契约需要继续固化

后续建议进一步明确：

- 各模式的输入参数最小必需集合；
- Hold / Resume / Abort 的严格语义；
- Direct 与 Recipe 的覆盖关系与优先级；
- 段切换后的上下文继承规则；
- 故障后是否允许自动恢复、何时需要人工确认。

---

## 6. 模块完整度与代码实现评估

| 模块 | 当前评估 | 完整度判断 | 主要问题 | 建议 |
| --- | --- | --- | --- | --- |
| `motion_control` | 对外主入口基本成形 | 高 | 文件过重；存在故障上报遗留调用；状态、命令、故障逻辑耦合 | 继续拆分内部子模块，补齐状态机测试 |
| `motion_planner` | 规划主链路存在 | 中高 | 需要进一步强化不同控制模式的语义边界与测试说明 | 增补模式差异测试和接口文档 |
| `pressure_controller` | 功能较完整 | 高 | 参数边界、现场整定、与主链路策略选择的基线文档仍需收敛 | 增加系统级回归与参数模板 |
| `pump_converter` | 已独立成模块 | 中高 | 需补充边界场景、配置合法性与嵌入式定点/精度评估 | 加强配置校验与换算测试 |
| `segment_completion` | 模块边界清晰 | 高 | 需继续验证各种结束条件组合场景 | 扩展组合场景测试 |
| `diagnostics` | 基础能力具备 | 中 | 更像监视层，尚缺工业化判据策略 | 增加持续时间、滞回、抑制与级别管理 |
| `state_reporter` | 结构合理 | 中高 | 当前与 `motion_control` 的故障调用接口未完全统一 | 统一 fault/report API 与封装入口 |
| `protection_manager` | 方向正确 | 中 | 保护动作与诊断策略联动仍需强化 | 明确保护分级与恢复机制 |
| `recipe_validator` | 边界较清晰 | 高 | 需继续收敛与实际运行链路的契约一致性 | 按接口契约补充验证规则 |
| `ramp_controller` | 作用明确 | 高 | 仍需更多压力切换场景验证 | 增加段切换/大步阶测试 |
| `rbf_pid` | 具备独立模块能力 | 中高 | 量产默认策略、参数模板与系统回归尚未基线化 | 作为增强策略纳入专项验证 |
| `tests/` | 覆盖面较广 | 中高 | 当前被构建问题阻断；故障路径、噪声、滞回、回滚类测试仍不足 | 先恢复全绿，再补负面与长期回归 |

---

## 7. 代码质量与风险审查

## 7.1 P0 阻断问题

### P0-01：`HDY_ReportFault` 未定义，导致链接失败

现象：

- `motion_control.c` 中多处仍调用 `HDY_ReportFault(...)`；
- 但当前代码已迁移到 `state_reporter` 的故障上报接口；
- 遗留调用未清理，导致链接阶段失败。

影响：

- 阻断全量构建；
- 阻断回归测试；
- 说明重构收口未完成。

处理建议：

- 统一改为 `HDY_StateReporter_ReportFault(...)`，或补回内部静态包装函数；
- 修复后立即执行全量构建与 CTest 验证；
- 同时检查其他类似的重构遗留调用。

## 7.2 P1 重要问题

### P1-01：控制器主文件复杂度过高

风险：

- 未来改动容易引入回归；
- 故障定位成本高；
- 状态机语义不易验证。

### P1-02：诊断策略过于瞬态化

风险：

- 正常建立过程可能被误判为异常；
- HMI 告警噪声高；
- 不利于现场联调与售后定位。

### P1-03：故障与状态上报接口尚未完全统一

风险：

- 同一类故障可能通过不同路径写入；
- 时间戳、消息、严重级别处理不一致；
- 增加维护成本。

### P1-04：文档与代码不同步

风险：

- 联调方理解偏差；
- 测试与现场使用口径不一致；
- 发布说明难以形成可信基线。

## 7.3 P2 优化项

### P2-01：平台适配层仍需加强

建议：

- 参数化时间基与浮点精度；
- 允许裁剪字符串诊断；
- 增强资源占用可配置性。

### P2-02：构建产物纳入版本管理

当前 `out/build/` 下大量文件处于版本管理状态，会带来：

- 仓库噪声；
- 评审干扰；
- 合并冲突；
- 追踪源码变化困难。

建议尽快清理并完善忽略规则。

---

## 8. 嵌入式平台兼容性评估

## 8.1 已具备的兼容性优势

- 静态内存模型，适合资源受限平台；
- 结构体状态集中，适合周期扫描调用；
- 代码纯 C99，便于跨编译器移植；
- 没有明显的线程、堆、文件系统依赖。

## 8.2 仍需进一步增强的部分

- 平台配置项需要集中化；
- 精度与运算成本需要可配置；
- 时间戳策略需要支持不同硬件基准；
- 诊断输出需要支持轻量裁剪模式；
- 长时间运行稳定性与边界回归需要进一步加强。

结论：**当前架构已具备嵌入式部署基础，但距离“多平台稳定复用”的成熟状态仍有一段工程化加固工作需要完成。**

---

## 9. 完成度判断

从工程视角给出定性判断：

### 9.1 核心功能完成度：较高

已具备：

- 单实例 FB 主链路；
- 多段配方；
- 典型动作链路建模；
- 压力控制、泵速换算、状态与诊断输出；
- 自动化测试基础。

### 9.2 工程化完成度：中等

尚缺：

- 稳定可构建基线；
- 统一的文档基线；
- 更工业化的诊断与保护策略；
- 更完善的平台配置与兼容性验证；
- 更细化的状态机与负面测试。

### 9.3 量产就绪度：暂未达到

主要原因并非“算法方向错误”，而是：

- 当前存在明确阻断缺陷；
- 文档、测试与接口契约仍需收敛；
- 诊断策略还偏开发态，不够现场化；
- 架构细节需要进一步瘦身与固化。

---

## 10. 修改建议与优先级

| 优先级 | 修改项 | 目标 | 输出物 |
| --- | --- | --- | --- |
| P0 | 修复 `HDY_ReportFault` 遗留调用 | 恢复全量构建 | 可通过构建的主干代码 |
| P0 | 清理构建产物版本管理问题 | 收敛仓库噪声 | 干净的源码变更集 |
| P0 | 重新执行全量构建与测试 | 恢复回归基线 | 通过的构建/测试记录 |
| P1 | 拆分 `motion_control` 内部职责 | 提升可维护性 | 内部子模块或清晰分层 |
| P1 | 重构诊断为“监视 + 判据”两层 | 提升现场可用性 | 新诊断策略与测试 |
| P1 | 统一故障与状态上报接口 | 消除重复与不一致 | 单一路径 fault/report API |
| P1 | 收敛接口契约与文档基线 | 保证联调一致性 | 更新后的 README / 设计文档 |
| P2 | 增加平台配置层 | 提升嵌入式适配性 | `hdy_config.h` 等配置机制 |
| P2 | 扩展噪声、抖动、长期回归测试 | 提升鲁棒性 | 新增测试用例与场景回归 |
| P2 | 形成发布基线与版本说明模板 | 提升交付能力 | 发布说明、计划与验收清单 |

---

## 11. 最终评审结论

**本项目当前的顶层架构方向是正确的，模块集合已较完整，控制能力也已达到“可继续工程化收敛”的阶段。**

但从专业工程角度判断，当前基线仍有以下关键短板：

1. 存在明确的构建阻断问题；
2. `motion_control` 仍然偏重；
3. 诊断模型还不够工业化；
4. 文档与代码基线不统一；
5. 平台适配与长期回归验证还需加强。

因此本次评审结论为：

> **有条件通过。**  
> 条件是：先完成基线修复与构建恢复，再按 Sprint 推进架构收敛、诊断工业化、嵌入式兼容增强与文档基线统一。

---

## 12. 建议后续配套文档

建议后续持续维护以下文档，以便跟踪：

- `项目评审报告.md`（本文件）
- `开发计划20260420.md`
- 发布说明 / 发布基线清单
- 关键接口契约说明（Direct / Recipe / Hold / Resume / Abort）
- HMI 诊断对照与保护动作矩阵
