# 项目需求与设计说明书

## 1. 项目背景

- 将旧PLC注塑机系统模块化、标准化改造。
- 采用 plcopen 规范及 IEC 编程模型，实现工艺层与运动控制层的清晰分离。
- 工艺层负责液压机理逻辑、阶段目标设置、阀门控制与段切换判断。
- 运动控制层仅负责纯数学计算与泵控速度输出，不直接控制电磁阀。

## 2. 需求分析

### 2.1 核心需求

- 运动控制层只作为数学与泵控计算器。
- 工艺层提供各段目标流量与目标压力。
- 运动控制层接收 `AxisREF` 实时反馈信息，输出平滑泵转速指令。
- 运动规划算法支持：
  - 位置基：`V = sqrt(2 * a * s)`
  - 时间基：`V = a * t`
- 支持三种控制模式：
  - 位置控制模式
  - 速度斜坡控制模式
  - 压力闭环控制模式
- 提供状态反馈与诊断接口。
- 支持合模、开模、顶针、座台、抽芯、射胶、射退等场景。
- 能处理多段目标，如合模多段、射胶分段注射及多级保压。

### 2.2 业务场景

- 合模场景：慢速合模、快速合模、低压模板、高压锁模。
- 射胶场景：注射阶段、多段注射、保压阶段、射退阶段。
- 其他场景：开模、顶针、座台、抽芯等定位与速度控制场景。

## 3. 架构设计

### 3.1 总体分层

1. 设备层
   - 伺服泵命令输出
   - 传感器状态读取
   - 电磁阀与液压机理接口（由工艺层处理）
2. 运动控制层
   - `MotionPlanner`
   - `MotionController`
   - `PumpConverter`
   - `StateReporter`
3. 工艺层
   - 动作配方管理
   - 阶段切换策略
   - 机理动作与阀门控制
   - 目标下发与反馈处理

### 3.2 职责边界

- 运动控制层：目标曲线规划、流量转速换算、状态与诊断输出。
- 工艺层：多段配方定义、阶段逻辑、机理执行、段切换决策。

### 3.3 核心数据结构

- `AxisREF`
  - 当前位移、速度、流量、压力、时间戳
- `MotionSegment`
  - 段名称、类型、规划算法、控制模式、结束条件
  - 目标位置、目标流量、目标压力、加速度、最大速度、时长、容差
- `Recipe`
  - 多段 `MotionSegment` 序列
  - 适用于合模、射胶、保压、开模等多段动作

## 4. 运动控制库设计

### 4.1 核心模块

- `MotionPlanner`
  - 位置基和时间基速度规划算法
  - 生成连续、平滑的速度曲线
- `MotionController`
  - 根据控制模式选择计算策略
  - 位置控制、速度斜坡、压力闭环
- `PumpConverter`
  - 将目标流量转换为伺服泵转速
  - 支持边界限幅
- `StateReporter`
  - 提供当前段、当前状态、诊断信息

### 4.2 控制模式

- 位置模式：以目标位移为基准，适用于合模、顶针等定位运动。
- 速度斜坡模式：以加速度生成平滑速度调节，适用于开模、射退等速度控制段。
- 压力闭环模式：使用实时压力反馈调节流量，适用于保压、锁模阶段。

### 4.3 多段支持

- 运动控制库应支持多段 `Recipe` 执行，并对段间切换提供完成信号。
- 工艺层负责按段切换，并提供后续段目标与机理动作。

### 4.4 诊断与反馈

- 提供压力误差、流量误差、速度误差等诊断项。
- 提供过压、欠压、流量偏差、位置偏差、超时等状态。
- 工艺层可基于诊断信息决定是否报警、暂停或切换段。

## 5. 工艺配方与运动控制关联

### 5.1 典型调用流程

1. 工艺层生成 `Recipe`。
2. 取出当前段 `MotionSegment`。
3. 调用运动控制库 `StartSegment`。
4. 周期性更新 `AxisREF`。
5. 运动控制库返回 `pump_speed_command`、`segment_status`、`diagnostic_info`。
6. 工艺层决定是否继续当前段或切换下一段。

### 5.2 合模场景

- 分段建模：慢速合模、快速合模、低压模板、高压锁模。
- 每段由工艺层定义目标流量/压力与结束条件。
- 运动控制层仅负责平滑转速输出。

### 5.3 射胶场景

- 建议分为注射段和保压段。
- 注射段使用位置基或时间基规划。
- 保压段使用压力闭环控制。

## 6. 开发计划与 Sprint 划分

### Sprint 1：需求确认与接口设计

- 完成数据模型与接口定义。
- 验收：确认 `AxisREF`、`MotionSegment`、`Recipe` 结构。

### Sprint 2：核心运动控制实现

- 实现位置基与时间基规划。
- 实现速度斜坡与流量转速换算。

### Sprint 3：压力闭环与多段执行

- 实现压力闭环模式。
- 实现多段状态机与诊断上报。

### Sprint 4：工艺层集成与场景验证

- 开发典型工艺配方。
- 验证合模、射胶、保压等端到端流程。

### Sprint 5：文档与稳定性测试

- 完成接口文档、测试报告与异常处理规范。
- 验收：关键动作场景测试通过。

## 7. 结论

- 该方案合理，可行。
- 运动控制层作为纯数学与泵控模块存在。
- 工艺层负责阶段目标与机理逻辑。
- 重点在多段配方、阶段切换、压力/流量控制模式。
