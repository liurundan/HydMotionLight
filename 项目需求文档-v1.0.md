# HDY 注塑机运动控制库项目需求文档

> 版本：v1.0
> 更新日期：2026-04-21
> 文档性质：项目需求整理与规范统一文档

---

## 1. 项目背景与目标

### 1.1 项目背景

本项目为面向注塑机液压场景的轻量级运动控制库，旨在将传统PLC注塑机系统进行模块化、标准化改造。项目采用PLCopen规范及IEC编程模型，实现工艺层与运动控制层的清晰分离，提供纯C99、静态内存、易于嵌入式部署的实现方案。

### 1.2 核心设计理念

**职责分离原则**：
- **工艺层**：负责液压机理逻辑、阶段目标设置、阀门控制与段切换判断
- **运动控制层**：仅负责纯数学计算与泵控速度输出，不直接控制电磁阀

**模块化设计**：
- 采用PLCopen函数块风格，命令触发后再周期调用刷新输出
- 保持算法模块与接口模块的清晰边界
- 支持多实例复用，一个FB实例只管理一个执行对象

### 1.3 技术约束

- **语言标准**：纯C99实现，无C++依赖
- **内存管理**：静态内存，固定上限，无动态分配
- **平台兼容**：无操作系统依赖，适合PLC/嵌入式控制器周期调用
- **命名规范**：统一`HDY_`前缀，保持代码一致性

---

## 2. 功能需求

### 2.1 核心功能需求

#### 2.1.1 运动控制能力
- **位置控制模式**：支持基于位置的目标控制，使用速度规划算法
- **速度斜坡控制模式**：支持基于时间梯度的速度控制
- **压力闭环控制模式**：支持基于压力反馈的闭环控制，目标压力斜坡平滑
- **运动规划算法**：
  - 位置基：`V = sqrt(2 * a * s)`（带制动力保护）
  - 时间基：`V = a * t`（支持位置目标约束时的制动包络）

#### 2.1.2 配方管理能力
- **多段配方支持**：支持最多`HDY_MAX_SEGMENTS`（16段）的配方执行
- **段类型支持**：合模、开模、顶针、座台、抽芯、射胶、射退、保压等
- **段参数完整性**：
  - 段标签（segmentTag）、类型、控制模式、结束条件
  - 目标位置、目标流量、目标压力
  - 加速度、最大速度、最大流量
  - 容差参数（位置、压力、流量、速度）
  - 超时限制、持续时间

#### 2.1.3 段切换与状态管理
- **显式启动**：通过`StartSegment()`命令显式启动指定段
- **段完成判定**：支持位置、时间、压力、流量、手动等多种结束条件
- **段切换信号**：提供`SEGMENT_COMPLETED`保持信号和`SEGMENT_CHANGED`单周期脉冲
- **配方完成**：最后一段完成后进入`FINISHED`状态
- **多段推进**：支持`NextSegment()`推进到配方下一段

#### 2.1.4 双模式参数来源
- **Recipe模式**：基于`RECIPE[]`数组的多段执行，支持`NextSegment()`推进
- **Direct模式**：基于`DIRECT_SEGMENT`的单段直接执行，完成后直接进入`DONE`
- **来源锁存**：启动后锁定活动段来源，运行中外部修改不影响当前执行
- **模式选择**：通过`USE_RECIPE`标志选择参数来源

### 2.2 压力控制需求

#### 2.2.1 压力控制器类型
- **P控制器**：基础比例控制
- **PI控制器**：带积分抗饱和的比例积分控制
- **PID控制器**：带微分滤波和抗饱和的完整PID控制
- **RBF-PID控制器**：自适应神经网络PID控制（预研功能）

#### 2.2.2 压力控制特性
- **目标斜坡平滑**：通过`RampController`实现压力目标阶跃抑制
- **前馈流量**：`targetFlow`作为前馈基值/名义保压流量
- **测量滤波**：可配置压力测量滤波系数
- **微分滤波**：可配置微分项滤波系数
- **死区处理**：支持压力控制死区设置
- **抗积分饱和**：积分项限幅防止积分饱和
- **无扰切换**：段切换时积分状态跟踪处理

### 2.3 诊断与保护需求

#### 2.3.1 诊断项目
- **配方诊断**：配方为空、段数超限、参数非法
- **命令诊断**：启动上下文非法、命令不被允许、段号越界
- **执行诊断**：超时、超压、欠压、流量偏差、位置偏差、速度偏差
- **传感器诊断**：反馈值非法、压力为负、时间戳倒退
- **内部诊断**：索引异常、状态损坏等内部错误

#### 2.3.2 诊断分级
- **INFO**：提示信息，不影响操作
- **WARNING**：预警信息，可能伴随降额操作
- **FAULT**：故障停机，需要检查和复位

#### 2.3.3 保护动作
- **NONE**：无强制动作，仅状态提示
- **WARNING**：提示，黄色状态栏/报警列表记录
- **DERATE**：降额运行，显示降额状态
- **STOP**：停止，红色故障弹窗，设备保护停机

#### 2.3.4 诊断保留机制
- **实时诊断**：`DIAGNOSTIC`当前周期诊断，自动清除
- **诊断锁存**：`DIAGNOSTIC_LATCH`最近一次非NONE事件
- **诊断快照**：`LAST_DIAGNOSTIC_SNAPSHOT`最近诊断事件上下文
- **故障快照**：`LAST_FAULT_SNAPSHOT`最近故障停机快照
- **诊断历史**：`DIAGNOSTIC_HISTORY`单快照模型，仅保留最近一条快照与累计事件计数

### 2.4 泵速换算需求

- **流量到泵速转换**：通过`FLOW_TO_PUMP_SPEED_GAIN`系数转换
- **输出限幅**：通过`PUMP_SPEED_LIMIT`限制最大泵速
- **非负输出**：`PUMP_SPEED`始终为泵侧非负幅值
- **方向解耦**：运动方向由`plannedDirection`和工艺层阀动作共同决定

### 2.5 嵌入式适配需求

- **静态内存**：所有数据结构使用固定大小数组
- **有界资源**：最大段数等有明确上限
- **可配置类型**：支持`float`/`double`精度切换
- **可裁剪特性**：诊断历史、标志位、压力环遥测等可通过 `hdy_config.h` 功能开关裁剪
- **周期调用友好**：状态机设计适合周期任务调用

---

## 3. 非功能需求

### 3.1 性能需求
- **实时性**：单周期执行时间满足PLC周期要求（通常1-10ms）
- **确定性**：无动态内存分配，执行时间可预测
- **效率**：避免复杂浮点运算，适合嵌入式平台

### 3.2 可靠性需求
- **安全输出**：`EN=false`、`Abort()`、故障状态下立即输出安全零值
- **状态机严谨**：显式状态机，避免状态漂移
- **参数校验**：配方装载和运行时配置严格校验
- **故障隔离**：单个实例故障不影响其他实例

### 3.3 可维护性需求
- **模块化设计**：算法模块与接口模块清晰分离
- **代码规范**：统一命名规范，清晰的代码结构
- **文档完整**：接口文档、设计文档、诊断映射文档齐全
- **测试覆盖**：单元测试、集成测试、场景测试完善

### 3.4 可扩展性需求
- **控制策略扩展**：支持新的压力控制器类型
- **诊断扩展**：支持新增诊断码和诊断类型
- **平台适配**：支持不同MCU/PLC平台的配置适配
- **多实例支持**：支持一个控制器管理多个运动轴

---

## 4. 接口需求

### 4.1 函数块生命周期

```
Init → LoadRecipe/LoadDirectSegment → StartSegment → Cycle/Scan/Execute → Complete/Abort
```

### 4.2 核心接口函数

#### 4.2.1 初始化与配置
- `HDY_MotionControlFB_Init()`：完整重置配置、配方、运行态和内部辅助状态
- `HDY_MotionControlFB_LoadRecipe()`：验证并装载配方数组，不自动启动
- `HDY_MotionControlFB_LoadDirectSegment()`：装载单段直接执行缓冲，不自动启动
- `HDY_MotionControlFB_ClearDirectSegment()`：清除直接段缓冲，不影响配方

#### 4.2.2 命令控制
- `HDY_MotionControlFB_StartSegment()`：验证并排队启动命令，实际状态转换在下次周期执行
- `HDY_MotionControlFB_NextSegment()`：验证并排队推进到下一段命令
- `HDY_MotionControlFB_Hold()`：排队保持命令，应用安全零输出
- `HDY_MotionControlFB_Resume()`：排队恢复命令，继续当前段执行
- `HDY_MotionControlFB_Abort()`：排队中止命令，应用安全输出
- `HDY_MotionControlFB_AcknowledgeDiagnostics()`：清除保留诊断（故障态除外）

#### 4.2.3 周期执行
- `HDY_MotionControlFB_Cycle()`：执行已采样的待处理命令和显式状态机
- `HDY_MotionControlFB_Scan()`：采样边沿触发命令输入（如`START_SEGMENT`），然后调用`Cycle()`
- `HDY_MotionControlFB_Execute()`：兼容周期入口，当前等价于`Scan()`

### 4.3 状态输出

#### 4.3.1 主要状态标志
- `ACTIVE`：仅在已启动段在当前周期执行时为真，装载配方不自动置位
- `BUSY`：标准PLCopen进度输出，运行中、保持、段完成等待时为真
- `DONE`：标准正常完成输出，仅`FB_STATE=DONE`时为真
- `ERROR`：仅故障级诊断时为真，警告和信息事件不触发
- `ERROR_ID`：镜像`ERROR=true`时的活动故障诊断码
- `FINISHED`：最后一段完成后为真
- `FAULT`：嵌入式面向故障输出，超时或配置损坏时断言
- `STATUS`：聚合控制器状态输出，用于HMI/上层集成
- `FB_STATE`：显式框架层状态机输出

#### 4.3.2 段状态信号
- `SEGMENT_COMPLETED`：段完成时保持，直到上层切下一段或进入最终完成态
- `SEGMENT_CHANGED`：段启动后单周期脉冲
- `STATE.segmentSource`：当前/最近活动段来源（`NONE/RECIPE/DIRECT`）

#### 4.3.3 执行输出
- `PUMP_SPEED`：泵转速指令，非负幅值
- `STATE.plannedVelocity`：规划速度，带符号
- `STATE.plannedFlow`：规划流量，非负幅值
- `STATE.commandedPumpSpeed`：命令泵速，非负幅值
- `STATE.plannedDirection`：规划方向，工艺层配合阀动作

### 4.4 诊断输出

#### 4.4.1 实时诊断
- `DIAGNOSTIC.code`：当前主要诊断码
- `DIAGNOSTIC.severity`：严重级别
- `DIAGNOSTIC.source`：来源模块
- `DIAGNOSTIC.recovery`：恢复建议
- `DIAGNOSTIC.protectionAction`：保护动作
- `DIAGNOSTIC.flags`：诊断标志位摘要

#### 4.4.2 保留诊断
- `DIAGNOSTIC_LATCH`：最近一次非NONE事件
- `LAST_DIAGNOSTIC_SNAPSHOT`：最近诊断事件快照
- `LAST_FAULT_SNAPSHOT`：最近故障停机快照
- `DIAGNOSTIC_HISTORY`：诊断历史记录

### 4.5 命令合法性矩阵

| 命令 | 允许状态 | 说明 |
| --- | --- | --- |
| START | IDLE / READY / SEGMENT_COMPLETE / DONE / ABORTED | 启动指定段 |
| NEXT | SEGMENT_COMPLETE | 推进到配方下一段 |
| HOLD | STARTING / RUNNING | 保持当前段，应用安全输出 |
| RESUME | HOLD | 恢复当前段执行 |
| ABORT | STARTING / RUNNING / SEGMENT_COMPLETE / HOLD | 中止执行，应用安全输出 |
| ACK | DISABLED / IDLE / READY / SEGMENT_COMPLETE / HOLD / DONE / ABORTED | 清除保留诊断 |
| RESET | 任意状态 | 下次周期执行完整重初始化 |
| STOP | （保留，拒绝） | 尚未实现，当前拒绝 |

---

## 5. 业务场景需求

### 5.1 合模场景
- **分段建模**：慢速合模、快速合模、低压模板、高压锁模
- **控制要求**：
  - 慢速合模：位置模式，低速稳定接近
  - 快速合模：速度斜坡模式，快速推进
  - 低压模板：压力模式，低压保护
  - 高压锁模：压力模式，高压稳定锁定

### 5.2 射胶场景
- **分段建模**：多段注射、保压阶段、射退阶段
- **控制要求**：
  - 注射段：位置或时间模式，按工艺要求推进
  - 保压段：压力闭环模式，稳定保压
  - 射退段：速度斜坡模式，控制退回速度

### 5.3 其他场景
- **开模**：速度斜坡模式，控制开模速度
- **顶针**：位置模式，精确顶出位置
- **座台**：位置模式，控制座台位置
- **抽芯**：位置模式，控制抽芯动作

### 5.4 多段控制场景
- **合模多段**：支持多个合模子段，每个段独立参数
- **多段注射**：支持多个注射段，不同速度和压力设置
- **多级保压**：支持多个保压段，逐级降压

---

## 6. 数据模型需求

### 6.1 核心数据结构

#### 6.1.1 反馈数据
```c
typedef struct {
    HDY_REAL position;   // mm
    HDY_REAL velocity;   // mm/s, signed by mechanism direction
    HDY_REAL flow;       // L/min, magnitude at pump side
    HDY_REAL pressure;   // MPa
    HDY_TIME timestamp;  // s
} HDY_AxisRef;
```

#### 6.1.2 运动段数据
```c
typedef struct {
    HDY_UINT8 segmentTag;       // 段标签（替代原name字段，节省RAM）
    HDY_SegmentType segmentType;
    HDY_PlannerType planner;
    HDY_ControlMode mode;
    HDY_EndConditionType endCondition;
    HDY_MotionDirection direction;

    HDY_REAL targetPosition;
    HDY_REAL targetFlow;
    HDY_REAL targetPressure;
    HDY_REAL maxAcceleration;
    HDY_REAL maxVelocity;
    HDY_REAL maxFlow;
    HDY_TIME duration;

    HDY_REAL tolerance;
    HDY_REAL positionTolerance;
    HDY_REAL pressureTolerance;
    HDY_REAL flowTolerance;
    HDY_REAL velocityTolerance;
    HDY_TIME timeoutLimit;

    HDY_REAL velocityToFlowGain;
    HDY_REAL pressureRampRate;

    HDY_PressureControllerType pressureController;
    HDY_REAL pressureKp;
    HDY_REAL pressureKi;
    HDY_REAL pressureKd;
    HDY_REAL pressureIntegralLimit;
    HDY_REAL pressureDeadband;
    HDY_REAL pressureFilterAlpha;
    HDY_REAL pressureDerivativeFilterAlpha;
    HDY_RbfPidConfig pressureRbfConfig;
} HDY_MotionSegment;
```

#### 6.1.3 运动状态数据
```c
typedef struct {
    HDY_UINT currentSegmentIndex;
    HDY_BOOL active;
    HDY_BOOL finished;
    HDY_BOOL faultActive;
    HDY_REAL plannedVelocity;
    HDY_REAL plannedFlow;
    HDY_REAL commandedPumpSpeed;
#if HDY_ENABLE_EXECUTION_REFERENCE
    HDY_ExecutionReference references;
#endif
    HDY_PressureControllerType pressureControllerApplied;
#if HDY_ENABLE_PRESSURE_LOOP_TELEMETRY
    HDY_PressureLoopState pressureLoop;
#endif
    HDY_ProtectionAction protectionAction;
    HDY_MotionDirection plannedDirection;
    HDY_SegmentSource segmentSource;
    HDY_ControllerStatus status;
    HDY_UINT8 currentSegmentTag;    // 替代原currentSegmentName
} HDY_MotionState;
```

### 6.2 枚举类型

#### 6.2.1 控制模式
- `HDY_MODE_POSITION`：位置控制模式
- `HDY_MODE_SPEED_RAMP`：速度斜坡控制模式
- `HDY_MODE_PRESSURE_CLOSED_LOOP`：压力闭环控制模式

#### 6.2.2 结束条件
- `HDY_END_POSITION`：位置结束
- `HDY_END_TIME`：时间结束
- `HDY_END_PRESSURE`：压力结束
- `HDY_END_FLOW`：流量结束
- `HDY_END_MANUAL`：手动结束

#### 6.2.3 运动方向
- `HDY_DIRECTION_EXTEND`：伸出方向
- `HDY_DIRECTION_RETRACT`：缩回方向
- `HDY_DIRECTION_HOLD`：保持方向

#### 6.2.4 段来源
- `HDY_SEGMENT_SOURCE_NONE`：无活动段
- `HDY_SEGMENT_SOURCE_RECIPE`：配方来源
- `HDY_SEGMENT_SOURCE_DIRECT`：直接段来源

#### 6.2.5 函数块状态
- `HDY_FB_STATE_DISABLED`：禁用态
- `HDY_FB_STATE_IDLE`：空闲态
- `HDY_FB_STATE_READY`：就绪态
- `HDY_FB_STATE_STARTING`：启动中
- `HDY_FB_STATE_RUNNING`：运行中
- `HDY_FB_STATE_SEGMENT_COMPLETE`：段完成
- `HDY_FB_STATE_HOLD`：保持态
- `HDY_FB_STATE_DONE`：完成态
- `HDY_FB_STATE_ABORTED`：中止态
- `HDY_FB_STATE_FAULT`：故障态

---

## 7. 质量需求

### 7.1 代码质量
- **编码规范**：遵循C99标准，统一代码风格
- **注释完整**：关键函数和数据结构有详细注释
- **模块独立**：各模块职责单一，耦合度低
- **错误处理**：完善的错误检测和恢复机制

### 7.2 测试质量
- **单元测试**：每个模块有独立单元测试
- **集成测试**：函数块级别的集成测试
- **场景测试**：典型注塑机场景测试
- **回归测试**：完整的回归测试套件
- **覆盖率**：关键路径代码覆盖率>80%

### 7.3 文档质量
- **需求文档**：清晰的功能需求和约束条件
- **设计文档**：完整的架构设计和模块说明
- **接口文档**：详细的函数接口和参数说明
- **诊断文档**：完整的诊断码和HMI映射
- **使用文档**：清晰的使用示例和集成指南

---

## 8. 约束与限制

### 8.1 当前不纳入发布承诺的内容

- **电磁阀直接控制**：由工艺层负责，库不直接控制
- **工艺层自动切段决策**：由工艺层判断，库提供完成信号
- **机理级安全联锁**：由工艺层实现，库提供诊断信息
- **RBF-PID主链路集成**：作为预研功能，非默认策略

### 8.2 平台限制

- **最大段数**：`HDY_MAX_SEGMENTS = 16`
- **诊断历史**：单快照模型（`HDY_DIAG_HISTORY_DEPTH` 已弃用，固定为1）
- **段标签**：`segmentTag` 使用 `uint8_t`，值0表示未设置
- **段名称**：`name[HDY_NAME_MAX]` 已被 `segmentTag` 替代（`HDY_NAME_MAX = 16` 保留用于兼容查询接口）

### 8.3 使用限制

- **单实例单对象**：一个FB实例只管理一个执行对象
- **不处理多缸协调**：多缸协调由工艺层处理
- **方向解耦**：库只计算运动，阀动作由工艺层控制

---

## 9. 验收标准

### 9.1 功能验收

- [ ] 所有控制模式正常工作
- [ ] 多段配方执行正确
- [ ] 双模式参数来源切换正常
- [ ] 段完成判定准确
- [ ] 诊断机制完善
- [ ] 泵速换算正确
- [ ] 压力控制稳定

### 9.2 性能验收

- [ ] 单周期执行时间满足要求
- [ ] 内存占用符合预期
- [ ] 长时间运行稳定

### 9.3 质量验收

- [ ] 代码审查通过
- [ ] 单元测试全部通过
- [ ] 集成测试全部通过
- [ ] 场景测试全部通过
- [ ] 文档完整且与代码一致

---

## 10. 附录

### 10.1 术语表

- **FB（Function Block）**：函数块，PLCopen编程模型的基本单元
- **Recipe**：配方，多段运动参数的集合
- **Segment**：段，单次运动控制的基本单位
- **PLCopen**：国际PLC开放组织，制定IEC61131-3标准
- **IEC61131-3**：国际电工委员会可编程控制器标准

### 10.2 参考资料

- PLCopen Motion Control Library Specification
- IEC61131-3 Programmable Controllers Standard
- 注塑机液压系统控制技术规范
- 嵌入式实时控制系统设计指南

---

**文档版本历史**：
- v1.0（2026-04-21）：初始版本，整理统一项目需求
- v1.1（2026-04-23）：同步代码变更 — 诊断历史从环形缓冲区简化为单快照模型；段名称改为段标签；修正数据结构定义与平台限制描述
